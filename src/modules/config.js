/*  3.1
    как работают экспорты и импорты

    создаём объект const config конфигурации позволяющий работать с переменными
    чтобы его экспортировать в index.js,  пишем - export const config = {

        продолжение в index.js,
*/

export const config = {
    key: '12345',
    new: 'new Str 1'
}